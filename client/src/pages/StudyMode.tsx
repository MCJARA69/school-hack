import type { Assignment } from '../api'
export function StudyMode(_props: { assignment: Assignment; onBack: () => void }) {
  return <div className="min-h-screen" style={{ background: '#0a0e1a' }} />
}
